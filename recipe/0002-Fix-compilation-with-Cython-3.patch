From 14f0ee760f9e4e675984a0b2ca669cfef6943f51 Mon Sep 17 00:00:00 2001
Date: Wed, 24 Apr 2024 04:59:06 +0300
Subject: [PATCH] Fix compilation with Cython 3

backport of https://github.com/flatsurf/surface-dynamics/pull/75
---
 surface_dynamics/interval_exchanges/iet_family.pyx | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/surface_dynamics/interval_exchanges/iet_family.pyx b/surface_dynamics/interval_exchanges/iet_family.pyx
index a591900..d5595b5 100644
--- a/surface_dynamics/interval_exchanges/iet_family.pyx
+++ b/surface_dynamics/interval_exchanges/iet_family.pyx
@@ -67,7 +67,7 @@ from sage.rings.integer cimport Integer
 # Beginning of the code
 ########################
 
-cdef int compare(const void * _left, const void * _right) nogil:
+cdef int compare(const void * _left, const void * _right) noexcept nogil:
     cdef mpz_t * left = (<mpz_t **> _left)[0]
     cdef mpz_t * right = (<mpz_t **> _right)[0]
     cdef int c = mpz_cmp(left[0], right[0])
-- 
2.44.0

